

#
# Define data related command mixin with autogenerated field and method accessors
#
command_mixins:
    nccs_category:
        aliases: ["nccs_category_optional"]
        class: NccsCategoryMixin
        mixins: ['name_resource']
        meta:
            nccs_category:
                data: nccs_category
                relations: true

    nccs_categorys:
        class: NccsCategoryMultipleMixin
        mixins: ['name_resource']
        meta:
            nccs_categorys:
                data: nccs_category
                relations: true

#
# Define data related base command
#
command_base:
    nccs_category:
        mixins: ["nccs_category"]
        server_enabled: True
        groups_allowed: ['non-profit-admin']

#
# Define data resource commands (list, get, save, remove, clear)
#
command:
    nccs_category:
        resource: nccs_category
        base: nccs_category
        base_name: nccs_category
        priority: 70