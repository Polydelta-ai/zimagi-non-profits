

#
# Define data related command mixin with autogenerated field and method accessors
#
command_mixins:
    nccs_sub_group:
        aliases: ["nccs_sub_group_optional"]
        class: NccsSubGroupMixin
        mixins: ['name_resource']
        meta:
            nccs_sub_group:
                data: nccs_sub_group
                relations: true

    nccs_sub_groups:
        class: NccsSubGroupMultipleMixin
        mixins: ['name_resource']
        meta:
            nccs_sub_groups:
                data: nccs_sub_group
                relations: true

#
# Define data related base command
#
command_base:
    nccs_sub_group:
        mixins: ["nccs_sub_group"]
        server_enabled: True
        groups_allowed: ['non-profit-admin']

#
# Define data resource commands (list, get, save, remove, clear)
#
command:
    nccs_sub_group:
        resource: nccs_sub_group
        base: nccs_sub_group
        base_name: nccs_sub_group
        priority: 70