

#
# Define data related command mixin with autogenerated field and method accessors
#
command_mixins:
    nccs_purpose:
        aliases: ["nccs_purpose_optional"]
        class: NccsPurposeMixin
        mixins: ['name_resource']
        meta:
            nccs_purpose:
                data: nccs_purpose
                relations: true

    nccs_purposes:
        class: NccsPurposeMultipleMixin
        mixins: ['name_resource']
        meta:
            nccs_purposes:
                data: nccs_purpose
                relations: true

#
# Define data related base command
#
command_base:
    nccs_purpose:
        mixins: ["nccs_purpose"]
        server_enabled: True
        groups_allowed: ['non-profit-admin']

#
# Define data resource commands (list, get, save, remove, clear)
#
command:
    nccs_purpose:
        resource: nccs_purpose
        base: nccs_purpose
        base_name: nccs_purpose
        priority: 70